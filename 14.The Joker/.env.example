# Environment Configuration Example
# Copy this file to .env and fill in your values
# DO NOT commit .env to git - it contains secrets!

# Server Configuration
NODE_ENV=development
PORT=5000

# CTF Challenge Flag (REQUIRED)
# Set this to your actual challenge flag in the .env file
# This is just a placeholder - replace with the real flag
# Example format: CTF{your_secret_flag_here}
CTF_FLAG=CTF{replace_this_with_your_actual_flag}

# Database (Optional - if using PostgreSQL)
# DATABASE_URL=postgresql://user:password@localhost:5432/dbname
