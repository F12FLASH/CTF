# Challenge Configuration - ENCRYPTED FLAG (Recommended)
# Generate an encryption key:   tsx scripts/encrypt-flag.ts --generate-key
# Encrypt your flag:             tsx scripts/encrypt-flag.ts "YOUR_FLAG" "YOUR_ENCRYPTION_KEY"
ENCRYPTION_KEY=your-64-character-hex-encryption-key-here
ENCRYPTED_FLAG=your-encrypted-and-obfuscated-flag-here
OBFUSCATION_KEY=your-custom-obfuscation-key

# Challenge Configuration - FALLBACK (Not Recommended for Production)
# Only used if ENCRYPTED_FLAG and ENCRYPTION_KEY are not set
FLAG=VNFLAG{TO_QUOC_GHI_CONG_VOI_NHAN_DAN_VIETNAM_9m2K7p1R4q8L3z6F0b5yXc}

# Security Configuration
# Comma-separated list of allowed origins for CORS
ALLOWED_ORIGINS=http://localhost:5000,https://yourdomain.com

# Server Configuration
PORT=5000
NODE_ENV=development
